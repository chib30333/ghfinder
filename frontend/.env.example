# Base URL of the ghfinder Fastify API.
# Leave BLANK for local dev — the Vite dev server proxies /api/* to
# http://127.0.0.1:8787 (see vite.config.ts), so the browser talks to the app
# same-origin. Set this only when the API is hosted elsewhere.
VITE_API_BASE_URL=
